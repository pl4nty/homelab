apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: sops-encrypted
  namespace: flux-system
spec:
  prune: false
  interval: 5m
  path: "kubernetes/aks"
  sourceRef:
    kind: GitRepository
    name: homelab-github
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg