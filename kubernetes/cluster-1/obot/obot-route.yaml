apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: obot
  namespace: default
spec:
  parentRefs:
    - name: ${CLUSTER_NAME}
      namespace: default
  hostnames:
    - obot.${PUBLIC_DOMAIN}
  rules:
    - backendRefs:
        - name: obot-obot
          port: 80
