apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: amazon-scraper
  namespace: toolhive-system
spec:
  groupRef:
    name: toolhive
  telemetryConfigRef:
    name: toolhive
  image: docker.io/library/node:22.23.1-bookworm@sha256:5647be709086c696ff32edaaf1c70cd26d1da6ab2b39c32f3c7b4c4a31957e37
  podTemplateSpec:
    spec:
      containers:
        - name: mcp
          command:
            - /bin/sh
            - -lc
          args:
            - npx -y github:pl4nty/mcp-server-amazon#make_npx_installable
          workingDir: /workspace
          volumeMounts:
            - name: amazon-cookies
              mountPath: /workspace/amazonCookies.json
              subPath: amazonCookies.json
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: amazon-cookies
          secret:
            secretName: toolhive
            items:
              - key: amazonCookies.json
                path: amazonCookies.json
        - name: tmp
          emptyDir: {}
  env:
    - name: HOME
      value: /tmp
    - name: NPM_CONFIG_CACHE
      value: /tmp/.npm
    - name: PUPPETEER_SKIP_DOWNLOAD
      value: "true"
    - name: EXPORT_LIVE_SCRAPING_FOR_MOCKS
      value: "false"
    - name: AMAZON_COOKIES_PATH
      value: amazonCookies.json
  secrets:
    - name: toolhive
      key: browser-ws-endpoint
      targetEnvName: BROWSER_WS_ENDPOINT
