# yaml-language-server: $schema=/workspaces/homelab/.devcontainer/helmrelease.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: tf2
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 3.1.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  timeout: 20m # slow startup
  values:
    controllers:
      main:
        type: statefulset
        pod:
          nodeSelector:
            kubernetes.io/arch: amd64
          securityContext:
            fsGroup: 1000
        statefulset:
          volumeClaimTemplates:
          - name: config
            accessMode: ReadWriteOnce
            size: 8Gi
            globalMounts:
              - path: /home/steam/tf-dedicated/tf/maps
        containers:
          main:
            image:
              repository: cm2network/tf2
              tag: latest@sha256:90c4d9bf0308bdf473f163adea6c5f901201618b1148f4ecdf31708a11229269
            env:
              SRCDS_HOSTNAME: "[AU] pl4nty's jungle GameTrackerClaimServer"
              SRCDS_REGION: 5
              SRCDS_MAXPLAYERS: 24
              SRCDS_PW: ""
              SRCDS_STARTMAP: plr_hightower
              SRCDS_MAPCYCLE: mapcycle.txt
            envFrom:
              - secretRef:
                  name: tf2
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
              startup:
                enabled: true
                spec:
                  failureThreshold: 900
            resources:
              requests:
                cpu: 30m
                memory: 900Mi
              limits:
                cpu: 1.5
                memory: 1500Mi
    configMaps:
      config:
        data:
          mapcycle.txt: |
            plr_hightower
    persistence:
      mapcycle:
        type: configMap
        name: "{{ .Release.Name }}-config"
        globalMounts:
          - path: /home/steam/tf-dedicated/tf/cfg/mapcycle.txt
            subPath: mapcycle.txt
    service:
      main:
        controller: main
        type: LoadBalancer
        sessionAffinity: ClientIP
        sessionAffinityConfig:
          clientIP:
            timeoutSeconds: 43200 # 12 hours
        annotations:
          external-dns.alpha.kubernetes.io/hostname: tf2.${PUBLIC_DOMAIN}
        ports:
          port1:
            port: 27015
            protocol: TCP
            primary: true
          port2:
            port: 27015
            protocol: UDP
          port3:
            port: 27020
            protocol: UDP
