apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kasm-proxy
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/pl4nty/kasm-nginx
      tag: latest@sha256:0d4017bfae7da4f5f0de4a7f6a56b1470366d7f5e552436c11e27fd81cb0f44c
    configMaps:
      files:
        enabled: true
        data:
          upstream_agent.conf: ''
          agent.conf: ''
    persistence:
      files:
        enabled: true
        type: configMap
        name: kasm-proxy-files
        mountPath: /etc/nginx/conf.d/upstream_agent.conf
        subPath: upstream_agent.conf
      files2:
        enabled: true
        type: configMap
        name: kasm-proxy-files
        mountPath: /etc/nginx/conf.d/services.d/agent.conf
        subPath: agent.conf
    #   config:
    #     enabled: true
    #     existingClaim: kasm-agent-config
    #     mountPath: /etc/nginx/conf.d/containers.d
    #     readOnly: true
    service:
      main:
        ports:
          http:
            port: 443
