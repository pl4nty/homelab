apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kasm-proxy
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/pl4nty/kasm-nginx
      tag: latest@sha256:902d88feb4d6af4818b47f90309d209ce304b874d6bf69160d877ad39122bf67
    persistence:
      config:
        enabled: true
        existingClaim: kasm-agent-config
        mountPath: /etc/nginx/conf.d/containers.d
        readOnly: true
    service:
      main:
        ports:
          http:
            port: 443
            