apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kasm-proxy
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/pl4nty/kasm-nginx
      tag: latest@sha256:155fdaab77f6384e64580753b2c4f72a127743c366d0175c4c1b1e65e91c76bc
    persistence:
      config:
        enabled: true
        existingClaim: kasm-agent-config
        mountPath: /etc/nginx/conf.d/containers.d
        readOnly: true
    service:
      main:
        ports:
          http:
            port: 443
            