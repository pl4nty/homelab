apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- kasm-cnpg.sops.yaml
- kasm-cnpg.yaml
- kasm-api.yaml
- kasm-manager.yaml
- kasm-init.yaml

secretGenerator:
- name: kasm
  namespace: default
  type: Opaque
  files:
  - api.app.config.yaml
- name: kasm-seed-data
  namespace: default
  type: Opaque
  files:
  - default_properties.yaml

generatorOptions:
  disableNameSuffixHash: true
