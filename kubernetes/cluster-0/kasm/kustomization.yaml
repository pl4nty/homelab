apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- kasm-api.yaml
- kasm-cnpg.sops.yaml
- kasm-cnpg.yaml

secretGenerator:
- name: kasm
  namespace: default
  type: Opaque
  files:
  - api.app.config.yaml

generatorOptions:
  disableNameSuffixHash: true
