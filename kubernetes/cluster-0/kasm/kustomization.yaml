apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ks.yaml
- kasm-cnpg.yaml
- kasm-redis.yaml

secretGenerator:
- name: kasm
  type: Opaque
  files:
  - api.app.config.yaml
