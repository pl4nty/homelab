apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: jsonwebtoken
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    image:
      repository: ghcr.io/pl4nty/jsonwebtoken
      tag: latest@sha256:521aac1c07c4975918789b572e50287dfa6e3dd9b805ca553a69dc3558c5f8a2
    env:
      SERVER_LOG_LEVEL: info
      SERVER_PORT: 8080
    service:
      main:
        ports:
          http:
            port: 8080
