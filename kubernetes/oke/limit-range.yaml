apiVersion: v1
kind: LimitRange
metadata:
  name: default
  namespace: default
spec:
  limits:
  - default:
      ephemeral-storage: 1Gi
    defaultRequest:
      ephemeral-storage: 1Mi
    type: Container
